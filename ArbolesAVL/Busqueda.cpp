/*
#include "Busqueda.h"
#include <iostream>

using namespace std;

int Busqueda::Nodo(int mat)  {
    numMat = mat;
    izqda = nullptr;
    drcha = nullptr;
}

void Busqueda::re_enorden() {
    if (izqda != nullptr)
        izqda->re_enorden();
    cout << "Matricula:   " << numMat << endl;
    if (drcha != nullptr)
        drcha->re_enorden();
}

*/
