/*
#include "Busqueda.h"
template <class elemType>

int Arreglo<elemType>::seqSearch(const elemType& item) const
{
    int loc;
    bool found = false;
    for (loc = 0; loc < length; loc++)
        if (list[loc] == item)
            {
                    found = true;
            break;
            }
    if (found)
        return loc;
    else
        return -1;
} //fin seqSearch
 */